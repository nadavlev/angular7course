import { SliderpipePipe } from './sliderpipe.pipe';

describe('SliderpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SliderpipePipe();
    expect(pipe).toBeTruthy();
  });
});
